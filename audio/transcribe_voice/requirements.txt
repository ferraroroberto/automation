# Core dependencies for voice transcription
numpy>=1.21.0
scipy>=1.7.0
sounddevice>=0.4.4
pyperclip>=1.8.0
pynput>=1.7.0
openai-whisper>=20230314
# PyTorch with CUDA support for RTX 50xx series GPUs (sm_120)
# Install with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
torch>=2.11.0.dev
imageio-ffmpeg>=0.4.5

# Optional: for GUI interface
# tkinter is included with Python, but for better compatibility:
# tk>=8.6

# Development dependencies (uncomment for development)
# Note: Install with: & .\.venv\Scripts\python.exe -m pip install -r requirements.txt
# pytest>=7.0.0
# black>=22.0.0
# mypy>=0.950
