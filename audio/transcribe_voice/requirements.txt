# Core runtime deps for the server-backed transcriber.
# Install with:  & .\.venv\Scripts\python.exe -m pip install -r requirements.txt

# Audio capture
numpy>=1.24
sounddevice>=0.4.6

# HTTP + config
requests>=2.31
PyYAML>=6.0

# Clipboard + hotkey
pyperclip>=1.8
pynput>=1.7

# Tray icon
pystray>=0.19
Pillow>=10.0

# Windows toast notifications (modern WinRT toasts, stack properly in Action
# Center — pystray's built-in balloon tips drop subsequent notifications when
# they arrive in quick succession).
winotify>=1.1; sys_platform == "win32"

# Nothing else.
# Intentionally removed from the legacy stack:
#   openai-whisper, torch, torchaudio, imageio-ffmpeg, scipy
